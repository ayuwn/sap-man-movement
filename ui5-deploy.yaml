# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: bsim.hcmapp.man.movement
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://sap-app-dv.cd.banksinarmas.com:8011
          client: '100'
        app:
          name: ZHR_MOVEMEN_MAN
          description: Movement
          package: ZHR01
          transport: S4DK900959
        exclude:
          - /test/
